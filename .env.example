# Конфигурация веб-хука для уведомлений об итогах синхронизации.
# Скопируйте в .env рядом со скриптом (.env в репозиторий не попадает).
# Без FSSYNC_WEBHOOK_URL уведомления отключены; токен необязателен.

FSSYNC_WEBHOOK_URL=https://example.com/hook
FSSYNC_WEBHOOK_TOK=секретный-токен
