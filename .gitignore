.mypy_cache
.pytest_cache
.ruff_cache
.venv
__pycache__

*.egg-info
build/
dist/

/docs
.fs-log

.env
*.env
# Демо-фикстура нормализатора (скрытый файл для проверки пропуска) — не секрет,
# должна оставаться под версионным контролем вопреки правилам .env выше.
!/examples/normalizer/08-edge-cases/hidden/.env

# Локальные правила Cursor — не для репозитория
.cursor/rules/*.local.mdc
